# Missing Dependencies to Install

Run this command to install the missing package:

```bash
npm install @react-native-community/netinfo
```

This package is required for network connectivity detection in the app.
